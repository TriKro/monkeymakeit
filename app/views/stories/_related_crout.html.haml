.related_story
  %a{ :href => related[:url] }
    .cover
      %img{:src => "#{related[:cover]}"}
    .related_titles
      .title
        "#{ related[:title] }"
      .subtitle
        = related[:subtitle]
    .clear
